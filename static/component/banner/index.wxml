<view class="banner" style="color: {{color}}; background:{{backgroundColor}}; border-color: {{borderColor}}" wx:if="{{display}}" bindtap="close">
  <span style="opacity: {{opacity}};">{{texts && texts.length ? texts[displayIndex] : ''}}</span>
</view>